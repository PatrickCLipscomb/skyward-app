import React from 'react';
import PropTypes from 'prop-types';

const Footer = (props) => (
  <div className="footer">
    Footer Content Here
  </div>
);

Footer.propTypes = {
  
}

Footer.defaultProps = {
  
}

export default Footer;