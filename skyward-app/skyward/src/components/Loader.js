import React from 'react';
import PropTypes from 'prop-types';

const Loader = (props) => (
  <div className="loader">
    Loader Present
  </div>
);

Loader.propTypes = {
  
}

Loader.defaultProps = {
  
}

export default Loader;